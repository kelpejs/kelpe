{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix examples/react-vite\" \"npm run dev --prefix packages/react\""
  },
  "prettier": {
    "singleQuote": true,
    "semi": false,
    "printWidth": 80
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
